{
    "name": "fabric-workspace-deployment",
    "$schema": "node_modules/nx/schemas/project-schema.json",
    "projectType": "application",
    "targets": {
        "build": {
            "executor": "nx:run-commands",
            "options": {
                "commands": [
                    "hatch run build:binary"
                ],
                "parallel": false
            }
        },
        "clean": {
            "executor": "nx:run-commands",
            "options": {
                "commands": [
                    "hatch run build:clean"
                ],
                "parallel": false
            }
        },
        "run": {
            "executor": "nx:run-commands",
            "dependsOn": [
                "build"
            ],
            "options": {
                "cwd": "dist",
                "commands": [
                    {
                        "command": "./hello-world-app",
                        "forwardAllArgs": true
                    }
                ]
            }
        }
    }
}